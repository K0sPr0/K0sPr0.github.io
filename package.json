{
  "devDependencies": {
    "eslint": "^8.22.0",
    "light-server": "^2.9.1"
  },
  "scripts": {
    "start": "node index.js",
    "build": "npm run build:css",
    "build:css": "copy src\\style.css build",
    "lint": "eslint src",
    "dev": "light-server -s . -p 9090 -w \"src\\style.css # npm run build:css # reloadcss\""
  },
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
